# SPDX-FileCopyrightText: 2026 James L. Burns and The PMForge Contributors
# SPDX-License-Identifier: GPL-3.0-or-later

version: "2"

linters:
  disable:
    # These linters currently report legacy baseline findings. Keep CI lint
    # deterministic while those findings are paid down in focused changes.
    - errcheck
    - staticcheck
    - unused
